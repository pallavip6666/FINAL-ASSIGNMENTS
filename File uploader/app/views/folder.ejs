<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <link rel="icon" href="../folder.png" type="image/x-icon" />

    <title>Your files</title>
  </head>
  <body>
    <header>
      <h1><%= folder.name %></h1>
    </header>

    <div class="container">
      <a class="back" href="/"> < go back </a>

      <div class="file-grid">
        <div class="file-header">
          <h3 class="file-header-name">Name</h3>
          <h3>Size</h3>
          <h3>URL</h3>
          <h3>Time</h3>
        </div>

        <% if (files) {%> <% files.forEach((file) => { %>
        <div class="file-card">
          <p class="file-name"><%= file.name %></p>
          <p><%= file.size %></p>
          <a href="<%= file.url %>">
            <p>Link</p>
          </a>
          <p><%= file.time %></p>
          <form
            action="/file/<%= file.id %>"
            method="POST"
            style="display: inline"
            class="delete"
          >
            <button
              type="submit"
              class="delete-btn"
              onclick="return confirm('Are you sure you want to delete this file?');"
            >
              <img src="../delete.png" alt="trash bin" />
            </button>
          </form>
        </div>
        <%})%> <% } else {%>
        <div>
          <p class="error">This folder is empty.</p>
        </div>

        <% } %>
      </div>
    </div>
    <footer>
      designed by
      <a id="github" href="https://github.com/dasha-solomkina"
        >dasha-solomkina</a
      >
    </footer>
  </body>
</html>
