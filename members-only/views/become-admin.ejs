<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>Become admin</title>
</head>
<body>
    <%- include("partials/menu.ejs") %>
    <div class="container text-center">
        <h1>Become Admin</h1>
        <div class="container-fluid w-50 mt-4">
            <% if (!locals.user.admin) { %>
                <form action="/become-admin" method="post">
                    <div class="mb-3">
                        <label for="secret" class="form-label">Enter secret passcode</label>
                        <input type="password" name="secret" id="secret" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-warning">Submit</button>
                </form>
            <% } else { %>
                <div class="alert alert-secondary">You are already an admin!</div>
            <% } %>
            <div class="text-center mt-4">
                <% if (success && typeof success === "boolean") { %>
                    <p class="alert alert-success"><%= message %></p>
                <% } else if (!success && typeof success === "boolean") { %> 
                    <p class="alert alert-danger"><%= message %></p>
                <% } %>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JavaScript and dependencies (jQuery and Popper.js) -->
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>